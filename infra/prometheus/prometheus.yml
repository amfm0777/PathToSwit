global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'telecomops-monitor'

scrape_configs:
  - job_name: 'telecomops-api'
    static_configs:
      - targets: ['api:8080']
    metrics_path: '/metrics'
